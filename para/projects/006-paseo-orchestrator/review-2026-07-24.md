---
phase: review
status: done
project: 006-paseo-orchestrator
date: 2026-07-24
verdict: approved
---

# Review: Retire the pi `subagent` tool; make Paseo the sole spawn layer

## Verdict: APPROVED

Reviewed `git diff fca2ee7..HEAD` (7 impl commits `2f30f1a`→`6342ce1`, plus the
lead's pre-spawn plan commit `88f304e`) against `plan-2026-07-24.md` and
`impl-2026-07-24.md`. All six scrutiny items pass; all twelve done criteria pass.
The one deviation from the plan's verbatim targets (`id` → `agentId`) is the
Step 10a-sanctioned field-name correction, applied in exactly the two files the
plan names.

## Plan conformance

- Step 1 (schema + role briefings): pass — `pi/orchestrator/worker-output-schema.json` and all four `pi/orchestrator/roles/*.md` match the plan's 1a/1b targets byte-for-byte (diff verified in-script). Commit `2f30f1a` touches only roles + schema.
- Step 2 (skills purge + janitor step): pass — `pi/orchestrator/skills/plan/references/closing-the-loop.md:5` is the exact 2a replacement; `pi/orchestrator/skills/janitor/SKILL.md:50-57` contains the 2b block verbatim, headers run 1–6 in order (`SKILL.md:12,21,35,42,50,59`), `paseo workspace archive` appears once.
- Step 3 (playbook rewrite): pass — all 12 kept sections of `pi/orchestrator/AGENTS.md` (including the preamble) are byte-identical to `fca2ee7`; 3a (`AGENTS.md:140`), 3b (`AGENTS.md:202-211`), 3c (`AGENTS.md:213-259`), 3d (`AGENTS.md:261-273`), 3e (`AGENTS.md:277`), 3f (`AGENTS.md:312-313`), and the three new 3g sections (`AGENTS.md:148,159,167`, placed between `Decision threshold` and `On session start`) all match the targets. The only 3c deltas are the two `agentId` corrections (`AGENTS.md:237,246`), sanctioned by Step 10a.
- Step 4 (registries): pass — `pi/orchestrator/model-registry-template.md` carries the new HTML comment, 4-column table, phase-defaults line, and Notes bullet verbatim; `para/resources/model-registry.md` has all 9 target table rows, the updated phase-defaults line, and the sol note updated to `pi/qwen-token-plan/qwen3.8-max-preview` with the other four notes preserved.
- Step 5 (resource doc + README + lesson): pass — `para/resources/paseo-orchestration.md` matches the 5a target byte-for-byte except the sanctioned `agentId` fix (`paseo-orchestration.md:56`); `README.md:13-20` (layout tree), `README.md:63` (intro sentence), `README.md:71-75` (bullets), and `README.md:79-87` (Testing) match 5b; `para/resources/lessons/smoke-testing-the-orchestrator.md:6` is the exact 5c blockquote, inserted after paragraph 1 and before `## What happened`.
- Steps 6–7 (checkpoints): pass — no commits, as specified.
- Step 8 (atomic switchover): pass — commit `42198bf` is exactly `index.ts` rewrite + the 7 deletions in one commit; `pi/orchestrator/index.ts` matches the 8a target byte-for-byte (40 lines).
- Step 9 (verification battery): pass — impl report records 9a–9f evidence; I independently re-ran 9a and 9b (below) and re-verified the CLI flags against live `paseo --help`.
- Step 10 (field correction + artifact): pass — `agentId` corrected in the two named files; corrections shipped in the report commit `6342ce1`, which Step 10 explicitly allows ("include the field-name corrections, if any, in this commit"); `impl-2026-07-24.md` has `status: done`.
- Ordering invariant: pass — commits `2f30f1a`, `75d67b5`, `3823616`, `2ffd319`, `dbdd529` touch no `.ts` file (docs/json only), so every intermediate commit loads by construction; the switchover is a single commit.
- Scope: pass — `git diff --name-status fca2ee7..HEAD` is exactly the in-scope list (24 paths) plus the plan commit `88f304e`. No out-of-scope file touched; `nixos/` and the other skills/lessons are untouched.

## Scrutiny items (from the plan's Maintenance notes)

1. **index.ts hooks preserved** — pass. The two hook registrations (`pi/orchestrator/index.ts:35-39`) are byte-identical to `fca2ee7:pi/orchestrator/index.ts:86-90` (confirmed with `od -c` and `diff`). No import from any deleted module (`grep 'from "\./' index.ts` → no matches); imports are only `node:fs`, `node:path`, `node:url`, `type ExtensionAPI` (`index.ts:11-14`). The changed comment above the hooks (`index.ts:31-34`) is the plan's dictated 8a text.
2. **Role frontmatter** — pass. All four roles parse as flat scalar YAML; every `description` containing a colon is double-quoted (`roles/explore.md:3`, `roles/plan.md:3`, `roles/implement.md:3`, `roles/review.md:3`); each carries `name`/`description`/`provider`/`thinking`/`workspace` and points at its skill by the stable absolute path `~/.pi/agent/extensions/orchestrator/skills/{name}/SKILL.md`.
3. **Paseo flags** — pass. `workspace create` uses `--mode branch-off` (`AGENTS.md:240`, `paseo-orchestration.md:27`), never `--worktree-mode`; `--worktree-mode` appears only in the verified-CLI-facts list of `run` flags (`paseo-orchestration.md:52`). Both confirmed against live `paseo workspace create --help` / `paseo run --help`. Background-run field is `agentId` (`AGENTS.md:237,246`, `paseo-orchestration.md:56`); workspace-create field is `workspaceId` (`AGENTS.md:242`). No `jq ` usage anywhere in scope (the only mention is the "`jq` is NOT installed" warning at `paseo-orchestration.md:52`); all JSON parsing uses `python3` one-liners.
4. **No stale references** — pass. `registerTool`, `git worktree add`, `pi --list-models`: zero matches in any in-scope file. Remaining `subagent`/`NIXPI` mentions are all verbatim-target provenance, not operational references: `README.md:79` ("the retired `subagent` tool"), `paseo-orchestration.md:3,56`, `index.ts:8` ("no NIXPI_* env machinery"), and the smoke-testing lesson (`smoke-testing-the-orchestrator.md:6` is the verbatim retirement blockquote; the gotcha-#2 body at lines 19–37 is deliberately retained and explicitly marked retired by it — distillation is deferred to the janitor per the plan's Maintenance notes). The done-criteria greps (`pi/orchestrator/*.ts`; `roles/` + `skills/` + `AGENTS.md`) return zero matches.
5. **Scope discipline** — pass (see Plan conformance → Scope).
6. **Verbatim fidelity** — pass. Every dictated target was reproduced exactly; the sole deviation is the sanctioned `agentId` correction (Step 10a) in `AGENTS.md:237,246` and `paseo-orchestration.md:56`.

## Verification evidence (reviewer-run)

- Runtime load smoke (done criterion): `pi --no-extensions -e ./pi/orchestrator/index.ts -p --no-session --model qwen-token-plan/qwen3.6-flash --thinking off "Reply with exactly: ok"` → `ok`, exit 0 (2.1s).
- Playbook-injection smoke: same invocation asking for the first philosophy principle → `KISS`, exit 0 (proves `before_agent_start` injects the new playbook).
- `git ls-files pi/orchestrator/` → `index.ts` is the only `.ts` file; none of the 7 deleted modules tracked.
- Skills count: 7 (`explore, grill, implement, janitor, plan, review, teach`).
- `ls paseo.json` → not found (correctly not created).
- Live `paseo workspace create --help` shows `--mode <mode>` with `branch-off`; `paseo run --help` shows `--worktree-mode` belongs to `run` — matches the docs.
- Paseo liveness / background-run field names (9d/9f) were not re-run by this review (a reviewer does not spawn agents); accepted on the impl report's recorded evidence, consistent with the lead's verified facts in the task brief.

## Standards findings

None. `index.ts` is 40 lines with one 6-line function; naming is specific; the header comment and the hook comment carry intent and provenance; no duplication introduced; all new prose follows the Writing standards (the dictated targets already conformed and were reproduced).

## Suggestions (non-blocking)

- The janitor's deferred lesson distillation (per the plan's Maintenance notes) should also reconcile `smoke-testing-the-orchestrator.md` gotcha #2's body, which now survives only as retired provenance.
- On the next paseo version bump, re-verify `workspaceId`/`agentId` field names before trusting the playbook one-liners (`AGENTS.md:242,246`); the Maintenance note at `paseo-orchestration.md:56` already says this.

## Summary

A clean, literal execution of a tightly-dictated migration: verbatim targets reproduced byte-for-byte, the deletion is atomic, every commit left the extension loadable, and the single deviation is the one the plan pre-sanctioned. Confidence is high; the runtime smokes pass on the reviewer's own run. Safe to merge.
