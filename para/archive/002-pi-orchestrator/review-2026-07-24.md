---
phase: review
status: done
project: 002-pi-orchestrator
date: 2026-07-24
verdict: approved
---

# Review: Replace the herdr daemon with a pi-native subagent extension

## Verdict: APPROVED

Reviewed `bb90d4b..b80d008` on `main` (prep commit plus the ten 002 commits). The central claim of this project holds: `diff -u` of `pi/subagent/{index,agents}.ts` against the pi 0.81.1 store example (`/nix/store/94aic7lcf25dcqcyygbvzic0vzvzna14-pi-0.81.1/libexec/pi/examples/extensions/subagent/`) shows exactly six hunks, and those hunks are exactly the three planned patches. Herdr is fully retired, the four roles migrated cleanly, and the docs match the plan's specified text. All done-criteria re-run green during this review.

## Plan conformance

- Step 1 (vendor example): pass — `pi/subagent/` contains exactly `agents.ts` and `index.ts`; `grep -c 'name: "subagent"' index.ts` = 1. README/agents/prompts not copied.
- Step 2 (agents.ts thinking support): pass — `ALLOWED_THINKING_LEVELS` at `pi/subagent/agents.ts:9`, `thinking?: string` at `:18`, validated parse at `:66-68`, push at `:76`. The level set matches pi's documented `--thinking` values exactly (pi README.md:559: off, minimal, low, medium, high, xhigh, max).
- Step 3 (index.ts patches): pass — `--thinking` flag at `pi/subagent/index.ts:296`, spawn env at `:340`, nesting guard at `:464` with a WHY comment. `grep -c NIXPI_WORKER` = 2, `grep -c '"--thinking"'` = 1. All three modes route through `runSingleAgent` (call sites at `:557`, `:629`, `:671`), so the flag and worker env apply to single, parallel, and chain alike.
- Step 4 (role migration): pass — all four roles have `name:` first, no `skills:`/`prompt_mode:`; commit `2e6b844` is exactly `+name: X` / `-skills: X` / `-prompt_mode: append` per file, bodies and model/thinking/tools values untouched. All four bodies keep the `~/.pi/agent/skills/{name}/SKILL.md` cat instruction (`.pi/agents/*.md:11`).
- Step 5 (smoke): pass — re-run during this review: `Available agents: review (project), implement (project), plan (project), explore (project)`, exit 0. See the dogfood note below; the smoke needed `env -u NIXPI_WORKER` because this review session is itself a spawned worker.
- Step 6 (deletions): pass — `ls pi/` = `subagent` only; `.pi/agents/` = exactly the four keeper roles.
- Step 7 (flake retirement): pass — commit `9be4e46` removes all five herdr references (input block, outputs arg, let binding, systemPackages entry, activation block); `flake.lock` pruned 44 lines, deletions only, no unrelated rev churn; `configuration.nix:174` comment now names the subagent tool. `nix flake metadata` exits 0 with inputs `llm-agents` + `nixpkgs` only. `grep -rin herdr` across all in-scope paths: zero matches.
- Step 8 (AGENTS.md): pass — pipeline note at `AGENTS.md:101` and following paragraph, "Skills and roles" and "When the user says go" sections match the plan's specified text verbatim. No "herdr", no "pane", `grep -c subagent` = 5.
- Step 9 (README.md): pass — layout block, pin-update command, roles paragraph, and testing section match the plan verbatim; no "herdr".
- Step 10 (model registry): pass — spawned-vs-in-session note at `resources/model-registry.md:42`.
- Step 11 (final verification): pass — `git diff --name-status bb90d4b..4292220` touches only in-scope paths; commit `b80d008` adds only the impl artifact.

Documented deviations (all reported in `impl-2026-07-24.md`, all justified):

- Prep commit `bb90d4b` on main: the seven role files were untracked, so the branch could not edit them. Lead-approved; the commit is content-neutral (7 files added, 112 insertions, zero modifications).
- Step 6 ran before step 5: the example's `parseFrontmatter` is a strict YAML parser, and `janitor.md`'s description (colon+space) throws instead of being skipped. Deleting the non-keepers first was the minimal correct response.
- `nix flake lock` run with positional syntax from inside `nixos/`: the plan's `--flake` flag does not exist on `nix flake lock`/`update`. Correct fix.

## Standards findings

- [judgement] `pi/subagent/agents.ts:55` — `parseFrontmatter` runs outside the read-error try/catch, so one role file with invalid YAML frontmatter (e.g. `: ` in a description) crashes discovery for every `subagent` call instead of skipping that file. This is upstream behavior, not a local diff, and fixing it would break the "three patches only" rule, so it is correctly left alone. It is a real footgun for future role authors; the impl artifact's issue 2 is the only place it is recorded.
- [judgement] `README.md:44` — `nix flake update llm-agents --flake ./nixos-dev-env` will fail: `nix flake update` takes `[option...] inputs...` and has no `--flake` flag (verified via `nix flake update --help`; the impl artifact's issue 3 notes the same mistake in the plan). Pre-existing: the old README carried the same syntax with `herdr` added, and plan step 9 specified this text verbatim, so the implementer was right to follow it.

No hard violations. The patch code meets the standards it is subject to: the guard comment at `index.ts:463` says why, not what, and the thinking-level error at `agents.ts:68` includes the file path, the offending value, and the allowed set, per the error-message standard. The rewritten docs are concise, active, and free of herdr-era filler.

## Suggestions (non-blocking)

- Fix the README update command in a trivial follow-up: `cd nixos && nix flake update llm-agents`.
- Consider one line in AGENTS.md "Skills and roles" warning role authors that frontmatter is strict YAML (no `: ` inside unquoted values). Or defer until it actually bites; YAGNI either way.
- The 002 worktree and branch still exist (`git worktree list` shows `../nixpi-dev-os-002-pi-orchestrator`). Expected: removal is the janitor's job at project close, after this review lands.

## Summary

Clean, disciplined migration. The vendored extension differs from upstream by exactly the three planned patches (verified by full-file diff and, for the nesting guard and worker env, by live dogfood: this review ran as a `NIXPI_WORKER=1` child where the guard correctly suppressed the tool, and unsetting the variable restored registration and four-agent discovery). Confidence is high; the only follow-ups are the pre-existing README nix syntax and a documented upstream YAML footgun, neither blocking.
