---
phase: review
status: done
project: 004-subagent-model-param
date: 2026-07-24
reviewer: qwen3.8-max-preview
verdict: APPROVED-WITH-NITS
---

# Review: per-call `model` override for the `subagent` tool

## Verdict: APPROVED-WITH-NITS

The change is a faithful, minimal implementation of the settled design. All five
reviewer-focus items pass, all Done criteria verify independently, scope is clean
(only the two in-scope files changed), and the code meets repo standards. One
non-blocking wording nit on the tool description. Safe to merge.

Reviewed commits `04ec790` (code + docs) and `24343a0` (impl report) against plan
commit `c5ec1ba`, via `git diff c5ec1ba..04ec790`.

## Plan conformance

- Step 1 (`modelOverride` param): pass — `pi/orchestrator/index.ts:286`
  adds `modelOverride: string | undefined,` immediately after `cwd`, matching the
  new signature position.
- Step 2 (apply override): pass — `index.ts:309` computes
  `const effectiveModel = modelOverride?.trim() || agent.model;`; `index.ts:310`
  pushes `--model` only when truthy; `index.ts:325` sets `model: effectiveModel`.
- Step 3 (three callers): pass — `index.ts:594` (`step.model,`), `index.ts:667`
  (`t.model,`), `index.ts:710` (`params.model,`), each directly below its `cwd,`
  line (verified by reading 591-596, 664-669, 707-712).
- Step 4 (three schema fields): pass — `index.ts:452` (`TaskItem`), `index.ts:459`
  (`ChainItem`), `index.ts:477` (`SubagentParams`); `grep -c "Model override"` = 3.
- Step 5 (tool description): pass — `index.ts:503` documents the override and
  points at `pi --list-models`; `grep -c "pi --list-models"` in index.ts = 4. See
  nit below on the bad-value clause.
- Step 6 (override-wins smoke test): pass — independently re-parsed the retained
  evidence at `/tmp/smoke-override.jsonl`: `result.model=qwen-token-plan/qwen3.6-flash`
  (override beat the decoy frontmatter `qwen3.8-max-preview`), `exitCode=0`,
  `event.isError=false`.
- Step 7 (bad-model pass-through): pass (lead-accepted) — see below.
- Step 8 (docs section): pass — `resources/subagent-orchestration.md:34` adds
  "## Per-call model override", placed Role format (18) → Per-call (34) → Worktree
  contract (53); names the precedence and points at `pi --list-models`
  (`grep -c` = 1).
- Step 9 (gate + commit): pass — syntax check exit 0; `git status --short` clean;
  diff touches only the two in-scope files (13 + 19 insertions, 2 deletions).

## Reviewer-focus items (from the plan's maintenance notes)

1. Precedence exactly `override?.trim() || agent.model`, no extra fallback: PASS.
   `index.ts:309` is verbatim; no third term, no default string.
2. `--model` pushed only when the effective model is truthy: PASS. `index.ts:310`
   `if (effectiveModel) args.push("--model", effectiveModel);`.
3. Backfill line unchanged: PASS. `index.ts:383`
   `if (!currentResult.model && msg.model) currentResult.model = msg.model;` is
   byte-identical to the plan excerpt (was line 381, shifted by the two inserted
   lines); it is absent from the diff.
4. No validation, enumeration, or state added: PASS. `pi --list-models` appears
   only inside description strings (`index.ts:452,459,477,503`); no model set,
   cache, lookup, or `--list-models` invocation exists in code. Pure pass-through.
5. `thinking` and `tools` unchanged: PASS. `index.ts:311-312` are untouched
   context lines in the diff.

## Step 7 / bad-model behavior (independently verified)

I re-parsed the retained evidence at `/tmp/smoke-badmodel.jsonl` rather than
re-running the live test. It confirms the impl report exactly:

- `result.model = qwen-token-plan/this-model-does-not-exist-xyz` — the bogus
  string was forwarded unchanged (no validation, no rewrite).
- `result.exitCode = 0`, `result.stopReason = error`, `result.isError = true`,
  `event.isError = false`, content `Agent error: 404: {"message":"Model not exist."...}`.

This matches the code path: `isFailedResult` (`index.ts:195-196`, pre-existing and
unchanged) returns true on `stopReason === "error"`, so the single-mode path
(`index.ts:716-722`) returns `isError: true`. The plan's Step 7 parser read the
event envelope `e.isError` (false) and expected a non-zero `exitCode`; that
prediction is wrong for this pi version, which warns and forwards a custom model
id. Per the lead's authoritative decision this is a plan-prediction defect, not a
code defect, and I do not report the code as defective for it. The design intent
(pass-through; a bad value surfaces as an error) is met and verified.

## Scope verification

- `git diff c5ec1ba..04ec790 --name-only` lists exactly
  `pi/orchestrator/index.ts` and `resources/subagent-orchestration.md`.
- `agents.ts`: untouched (absent from diff).
- Role files under `pi/orchestrator/roles/`: untouched.
- `SingleResult` interface: untouched; still `model?: string` (`index.ts:162-175`).
- Only one `--model` push site and one `agent.model` read exist in the file, both
  inside `runSingleAgent`, both updated to `effectiveModel`. No model reference was
  missed.

## Done criteria (verified by reviewer, not trusted from the report)

- Syntax check `node --experimental-strip-types --check ...index.ts`: exit 0. ✓
- `runSingleAgent` param + `effectiveModel` compute/conditional-push/result-seed:
  `index.ts:286,309,310,325`. ✓
- Three callers pass per-call model after `cwd`: `index.ts:594,667,710`. ✓
- `grep -c "Model override"` = 3. ✓
- Tool description mentions override + `pi --list-models`: `index.ts:503`. ✓
- Step 6 evidence: `model=qwen3.6-flash`, `exitCode=0`, `isError=false`. ✓
- Step 7 evidence: `result.isError=true`, bogus model forwarded unchanged. ✓
- Docs section present and correctly placed: `subagent-orchestration.md:34`. ✓
- Backfill (`index.ts:383`) unchanged; `agents.ts`, roles, `SingleResult` untouched. ✓
- `git status --short` clean; only the two in-scope files modified. ✓

## Standards findings

- None blocking. The override is injected through a parameter (not a global),
  matching the dependency-injection rule; the type is explicit
  (`string | undefined`), no `any`; schema fields mirror the existing `cwd` style;
  new lines use tabs (`cat -A` confirms `^I`); names are specific and greppable
  (`effectiveModel` = 3 hits, `modelOverride` = 2 hits, both < 5).
- Context, not a finding against this change: `index.ts` is ~1055 lines, over the
  500-line guideline. This is pre-existing (the plan records it at 1044 lines) and
  the file is deliberately patched in place so it can be re-diffed against the pi
  store example on version bumps (maintenance notes). This change adds a net 11
  lines and keeps the patch minimal, which is the right call here.

## Suggestions (non-blocking)

- [nit] `index.ts:503` — the tool description says values are "passed straight to
  `pi --model`, which errors on a bad value." Strictly, in this pi version `pi
  --model` does not error: pi warns and forwards a custom model id, the provider
  404s (`model_not_found`), and the tool surfaces `isError=true`. The docs section
  (`subagent-orchestration.md:44-49`) uses the corrected, precise wording, so the
  two descriptions are slightly inconsistent about where the error originates. The
  observable outcome (bad value → error surfaced to the caller) is correct either
  way, and the impl report records that the lead confirmed this wording at Step 5.
  Wording-precision only; no behavior change needed. If touched later, align it
  with the docs section.

## Summary

High-quality, tightly scoped change: 11 net lines of code that do exactly what the
grill and plan specified, with no scope creep and no added validation or state. I
independently re-verified the syntax gate, every grep/count, the unchanged backfill
and out-of-scope files, and both smoke tests from the retained evidence. The only
blemish is a cosmetic wording inconsistency the lead already approved. Confidence
is high; no concerns for the human lead beyond the optional nit.
